"use strict";
console.log("2" > 1);
console.log(null > 0);
console.log(null == 0);
console.log( null >= 0);
/* when using < > >= <= with null , the null is assigned a value 0 
"==" only look for values but "===" checks for both datatype and value
comparison operator < > and equality check == both are different*/